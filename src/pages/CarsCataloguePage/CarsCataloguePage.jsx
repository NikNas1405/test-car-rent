const CarsCataloguePage = () => {
  return (
    <main>
      {/* {loading && <Loader />} */}
      {/* {error ? (
        <Error>
          Sorry. Something went wrong. Please reload the page to try again.
        </Error>
      ) : (
        <CarsList cars={allCars} /> )*/}
    </main>
  );
};

export default CarsCataloguePage;
